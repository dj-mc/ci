#include <math.h>
#include <stdio.h>

// TODO: Use argc and argv as input for a calculator

int main(int argc, char const *argv[]) {
  printf("The cosine of 1 is %0.3g\n", cos(1.0));
  printf("Bye!");
  return 0;
}
